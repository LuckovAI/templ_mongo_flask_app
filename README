Example full value backend wsgi application for web-server with flask and mongodb

Install
    1) move to the directory with source
    2) execute:
        sudo python setup.py install
        
run web-server:
    sudo python /mongo_flask/start.py

run tests:
    sudo python /tests/test.py
    
